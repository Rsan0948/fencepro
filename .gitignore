node_modules
dist
coverage
.vite
*.tsbuildinfo
.DS_Store
*.local
.vscode
.idea
.ruff_cache/

# Local Claude Code / agent tooling (machine-specific, not for public repo).
# Matches the calc_animator + digital-sangha public-repo hygiene pattern.
.claude/
.helicops/
.mcp.json
AGENTS.md
CLAUDE.md

# Internal-only orchestration artifacts. Lane briefs and orchestration
# planning docs live on disk for the orchestrator + implementation agents
# to reference, but never ship to the public repo.
docs/lane_briefs/

# Optional local data override — keep regional/branding data out of source.
# See docs/architecture.md §Data Layer.
src/data/local.ts

# HF Space working clone created by scripts/deploy-hf.sh. Local-only,
# rebuilt on every deploy. See docs/deploy-hf.md.
.hf-deploy/
